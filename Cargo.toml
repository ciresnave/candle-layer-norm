[package]
name = "candle-layer-norm"
version = "0.0.3"
edition = "2024"
license = "bsd-3"
description = "Layer Norm layer for the candle ML framework."

[dependencies]
candle = { git = "https://github.com/ciresnave/candle.git", branch = "main", package = "candle-core", features = ["cuda"] }
half = { version = "2.3.1", features = ["num-traits"] }

[build-dependencies]
anyhow = { version = "1", features = ["backtrace"] }
num_cpus = "1.15.0"
rayon = "1.7.0"
